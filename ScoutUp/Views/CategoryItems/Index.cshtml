@model IEnumerable<ScoutUp.Models.CategoryItems>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Categories.CategoryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategoryItemName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategoryItemPhoto)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Categories.CategoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryItemName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryItemPhoto)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.CategoryItemID }) |
                @Html.ActionLink("Details", "Details", new { id=item.CategoryItemID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.CategoryItemID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
